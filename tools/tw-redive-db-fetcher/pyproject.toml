[project]
name = "tw-redive-db-fetcher"
version = "1.0.0"
description = "TW Redive Master DB Fetcher"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "UnityPy>=1.10.0",
    "brotli>=1.0.0",
]
